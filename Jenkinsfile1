def artifactname = "devops-snow-build-app.jar"
def repoName = "JenkinsDevOpsProject"
def pipelineName = "SunilPipelines/Sunil-Scripted-Pipeline-Sonar"
def semanticVersion = "${env.BUILD_NUMBER}.0.0"
def packageName = "devops-snow-build-prasad_pkg_${env.BUILD_NUMBER}"
def version = "${env.BUILD_NUMBER}.0"
def pkgName = "devops-snow-build-prasad_pkg_${env.BUILD_NUMBER}"

pipeline {
  agent any
 
  stages {

       stage('Example1') {
            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins1-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins1-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
       	stage('Example2') {
            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins2-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins2-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
       	stage('Example3') {
            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins3-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins3-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
       	stage('Example4') {
            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins4-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins4-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
  	stage('Example5') {
            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins5-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins5-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
     stage('Example6') {
            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins6-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins6-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

     stage('Example7') {
            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins7-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins7-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

     stage('Example8') {
            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins8-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins8-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

     stage('Example9') {
            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins9-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins9-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

     stage('Example10') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins10-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins10-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
     stage('Example11') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins11-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins11-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
     stage('Example12') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins12-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins12-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

     stage('Example13') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins13-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins13-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

     stage('Example15') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins15-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins15-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
     stage('Example16') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins16-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins16-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
     stage('Example17') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins17-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins17-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

 stage('Example18') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins17-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins17-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
 stage('Example19') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins19-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins19-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
 stage('Example20') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins20-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins20-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

 stage('Example21') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins21-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins21-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
 stage('Example22') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins22-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins22-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

 stage('Example23') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins23-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins23-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

 stage('Example24') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins24-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins24-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}

 stage('Example25') {

            steps {
                echo 'Hello World'
            writeFile file: "/tmp/app-devops-jenkins25-${env.BUILD_NUMBER}-SNAPSHOT.jar", text: 'This is a test'
    
            }
            
            post { 
                always { 
        script { 
                 def server = Artifactory.server 'JFrog1'
                 def uploadSpec = """{
                    "files": [{
                       "pattern": "/tmp/app-devops-jenkins25-${env.BUILD_NUMBER}-SNAPSHOT.jar",
                       "target": "felipe-repo/${env.BUILD_NUMBER}/"
                    }]
                 }"""

              def buildInfo = server.upload(uploadSpec) 
              server.publishBuildInfo buildInfo   
        }}}
	}
      stage('Deploy') {
                 steps {
                    

		    snDevOpsChange(ignoreErrors:false)
                    
                 }
      }


 }
 
}
